{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the main window. App-defined commands are always callable; this grants the core defaults plus events, which S9's notification actions arrive on.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "core:event:default"
  ]
}
